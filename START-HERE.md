# 先讀這一頁

這是一份**可以直接用的電子名片**：手機掃 QR 或碰一下 NFC 貼紙就開啟，
支援繁中／英文／日文即時切換，對方按一下就能把你存進通訊錄。

純靜態網頁，**沒有建置步驟、不需要任何 AI 服務點數、不綁任何平台**。
放上 GitHub Pages 後，改內容只要改一個檔再 `git push`。

---

## 你需要什麼

- 一個 GitHub 帳號（免費方案就夠）
- Claude Code
- 一張你的照片、你公司的 logo（沒有也行，見下方）
- 想加 LINE 的話：你的 LINE 好友 QR Code 圖

---

## 三步驟

### 1. 把這包解開，用 Claude Code 打開它

```
cd digital-card-starter
claude
```

### 2. 把這句話貼給 Claude Code

> 請讀 SETUP.md，照著它把這份電子名片改成我的。需要什麼資料再問我。

它會問你姓名、職稱、公司、聯絡方式那些，你回答就好。
不確定的先略過，之後隨時能改。

### 3. 照它的指示上線

它會帶你建 GitHub repo、開啟 GitHub Pages、把網址做成 QR Code 與 NFC 貼紙。
大約 10 分鐘。

---

## 幾件先知道比較好的事

**`npm test` 一開始會有一項失敗，這是故意的。**
那一項在擋「還沒改完就發布」，把範例資料換成你自己的之後就會通過。

**永久入口網址是唯一不能隨便改的東西。**
NFC 貼紙與印出去的 QR 一律寫 repo 根目錄那一頁（`https://你的帳號.github.io/`），
它再轉到名片頁。這樣日後換內容、換平台、換網域，**已經發出去的貼紙與名片都不用重做**。

**沒有的東西可以不要。** 公司 logo、個人照、LINE、紙本名片掃描圖都是可選的，
`SETUP.md` 的「可選區塊」有教怎麼整段拿掉。

**手機一定要實機測。** 電腦上看起來對，不代表手機上對——
特別是「存到通訊錄」這一步，iPhone 與 Android 行為不同，兩支都要試。

---

## 檔案在哪

| 你會動到的 | 是什麼 |
|---|---|
| `card/config.js` | **名片內容的唯一真相**。姓名、職稱、聯絡方式、專長全在這裡 |
| `card/` 的圖檔 | logo、個人照、LINE QR、紙本名片掃描圖 |
| `index.html`（根目錄）| 永久入口轉址層，裡面三處網址要換成你的 |

| 你通常不用動的 | 是什麼 |
|---|---|
| `card/app.js` | 介面行為與三語用語 |
| `card/styles.css` | 樣式 |
| `card/vcard.js` | 產生通訊錄檔（vCard 3.0） |
| `card/save-strategy.js` | 「存到通訊錄」的逐級降級邏輯 |
| `tests/` | 34 項測試，擋住已知會踩的坑 |

詳細維護方式看 `README.md`，上線流程看 `SETUP.md`。
